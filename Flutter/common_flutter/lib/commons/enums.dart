enum CloseEnum{
  back,
  close,
}

enum ImageEnum {
  cslogo,
  landing,
  startlogo,
}

enum NavArgEnum {
  latitude,
  longitude,
}

enum PageEnum {
  home,
  landing,
  location,
}

enum PrefEnum {
  cordinates,
}

enum SVGEnum {
  imgerror,
  startlogo,
}
